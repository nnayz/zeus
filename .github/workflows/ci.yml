name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  engine:
    name: Swift engine (daemon + CLI)
    runs-on: macos-15
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
      - name: Validate shell scripts
        run: bash -n scripts/*.sh zeus/scripts/*.sh
      - name: Install and audit browser sidecar
        working-directory: sidecar
        run: |
          npm ci
          npm audit --omit=dev
      # SwiftPM block-buffers when stdout is a pipe, so a killed test process
      # loses most of its console output. The xunit file is written
      # incrementally and survives that.
      - name: Test
        run: swift test --xunit-output "${RUNNER_TEMP}/tests.xml"
      - name: Failing tests
        if: failure()
        run: |
          xmllint --xpath '//testcase[failure]/@name' "${RUNNER_TEMP}/tests.xml" 2>/dev/null \
            || echo "no per-test failure recorded — the test process died before finishing"
      - name: Release build
        run: swift build -c release

  app:
    name: Rust app (zeus)
    runs-on: macos-15
    timeout-minutes: 60
    defaults:
      run:
        working-directory: zeus
    steps:
      - uses: actions/checkout@v5
      # rust-toolchain.toml pins 1.95.0 with clippy + rustfmt.
      - name: Cache cargo registry and target
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            zeus/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('zeus/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-
      - name: Format
        run: cargo fmt --all -- --check
      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings
      - name: Test
        run: cargo test --workspace
      - name: Release build
        run: cargo build --workspace --release

  bundle:
    name: Package zeus.app (unsigned)
    runs-on: macos-15
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v5
      - name: Cache cargo registry and target
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            zeus/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('zeus/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-
      # package.sh lipos two slices into a universal binary; both targets have
      # to be installed or the x86_64 pass fails with "can't find crate for std".
      - name: Install universal targets and cargo-packager
        run: |
          rustup target add aarch64-apple-darwin x86_64-apple-darwin
          cargo install cargo-packager --locked
      - name: Package
        env:
          ZEUS_DIST_DIR: ${{ runner.temp }}/zeus-dist
        run: zeus/scripts/package.sh
      - name: Verify bundle structure and signature
        run: |
          app="${RUNNER_TEMP}/zeus-dist/zeus.app"
          plutil -lint "${app}/Contents/Info.plist"
          test -x "${app}/Contents/Resources/bin/zeusd"
          test -x "${app}/Contents/Resources/bin/zeusd-holder"
          test -x "${app}/Contents/Resources/bin/zeus"
          codesign --verify --deep --strict "${app}"

  browser-sidecar:
    name: Browser sidecar integration
    runs-on: macos-15
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
      - name: Install sidecar and browser engines
        working-directory: sidecar
        run: |
          npm ci
          npx playwright install chromium webkit firefox
      - name: Run browser integration tests
        env:
          ZEUS_RUN_BROWSER_TESTS: "1"
        run: swift test --filter BrowserPoolTests
